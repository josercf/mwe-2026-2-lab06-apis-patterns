[pytest]
# Rodar sempre da raiz do laboratório: `python3 -m pytest`
# `pythonpath` põe o pacote `app` do serviço de frete no caminho de importação
# sem exigir instalação nem PYTHONPATH exportado à mão.
pythonpath = . servicos/frete
testpaths = tests servicos/frete/tests
addopts = -q
